import React from 'react';

import { SignInScreen } from '@/features/auth/components/sign-in-screen';

/**
 * Routes hold no logic. The screen is composed in `src/features/auth`, so the
 * routing tree stays a map of the app rather than a place code accumulates.
 */
export default SignInScreen;
