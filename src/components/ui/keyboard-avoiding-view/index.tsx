/**
 * React Native's own, re-exported for the reason `scroll-view` is: a screen
 * should import its layout primitives from one place, and gluestack has no
 * wrapper for this one.
 */
export { KeyboardAvoidingView } from 'react-native';
